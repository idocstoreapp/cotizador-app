---
/**
 * Página de administración de precios
 * Permite modificar precios de materiales, servicios, muebles y variantes
 */
import Layout from '../../components/Layout';
import QueryProvider from '../../components/QueryProvider';
import AdminPreciosPage from '../../components/admin/AdminPreciosPage';
import { obtenerUsuarioActual } from '../../services/auth.service';

// Verificar autenticación y que sea admin
let usuario = null;
try {
  usuario = await obtenerUsuarioActual();
} catch (error) {
  console.error('Error al verificar usuario en precios (servidor):', error);
}

// No redirigir automáticamente - dejar que el componente Layout maneje la autenticación
---

<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="generator" content={Astro.generator} />
    <title>Administración de Precios - Mueblería Cotizador</title>
  </head>
  <body>
    <Layout client:load currentPath="/admin/precios">
      <QueryProvider client:load>
        <AdminPreciosPage client:load />
      </QueryProvider>
    </Layout>
  </body>
</html>

